apiVersion: v1
kind: Service
metadata:
  name: simple-app-service
  namespace: default
  labels:
    app: simple-app
spec:
  selector:
    app: simple-app
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP